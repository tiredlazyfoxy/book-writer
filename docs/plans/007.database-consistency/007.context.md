# Step 007 — step-specific context

D7 and the full MobX hard rules are in `context.md`.

Narrow to this step:

- **Page shape to mirror** — `docs/plans/006.llm-server-connections/005.frontend-base.md`
  (`LlmServersPage` + `llmServersPageState`): async trio only, `makeAutoObservable`,
  external `(state, …, signal)` effect fns with `runInAction`, `useState(() => new X())`,
  page-level mount-load / unmount-abort `useEffect`, per-row actions as inner handlers,
  modal/target flags component-local. `import * as dbApi from '../../api/db'`.
- **Nav growth** — 006 established `Users | LLM Servers` in `src/admin/App.tsx`; this step
  adds `Database`. Keep the minimal local layout the 004/006 admin work established; do not
  introduce a shared sidebar framework.
- **Export is fire-and-forget** — the download is a browser save side-effect from
  `dbApi.exportDatabase()`; no trio, no state field needed beyond maybe a transient
  "exporting" flag if desired.
- **Import refusal must be visible** — `importAction` catches the rejection from
  `importDatabase(file)` (a 400 refusal, US-019.AC-3) and writes it to a page error/status
  field the component renders; a successful import reloads the report.
- **Rebuild result** — `rebuildAction` shows the count/status the endpoint returns
  (Stage-1: 0 rows / possibly a no-provider status per step 004's frozen contract). The page
  should render whatever the DTO carries without assuming a non-zero count.
- **No route param / remount concern** — `/admin/database` is a static route with no id; a
  single mount-load suffices.
- No tests: no frontend test runner (context.md). All DoD `[manual/live]`.
