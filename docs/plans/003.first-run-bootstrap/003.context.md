# 003.context — Setup service + User codec

## What this step amends (not scope drift — see `context.md`)
- `services/db_import_export.py` — created by 001 step 004 with an **empty**
  `TABLE_REGISTRY`, `BATCH_SIZE = 100`, `export_all()`, and `import_all(zip_bytes)`
  (which calls `init_db()` first and `run_vector_rebuild()` after). This step
  **adds** the `User` codec pair and the first `TABLE_REGISTRY` tuple; it does
  not change the mechanism.
- `services/setup.py` is **new**.

## Reference anchors (adapt — decisions 3 & 4 override)
From `D:/GitRoot/_TextGens/LLMRPTextOnlyProject/backend`:

- `services/database.py` `create_database(username, password)` — `init_db()` →
  mint credentials → `db.users.create(admin)` → `set_db_ready(True)`, returns the
  admin. **Overrides:** BookWriter's `create_database` also takes
  `password_confirm` and does **service-side validation** (min-8, confirm-match,
  already-configured refusal) raising `SetupError` (decision 3); the reference
  validated in the route.
- `services/database.py` `import_database(zip_bytes)` — `import_all(zip_data)` →
  `run_vector_rebuild()` → `set_db_ready(True)`. **Override:** the reference does
  **no** validation (corrupt zip → uncaught → HTTP 500); BookWriter wraps it and
  raises `SetupError` on failure **without** calling `set_db_ready` (decision 4,
  required by `US-002.AC-2`). Note `import_all` already invokes `init_db()` and
  the vector rebuild internally, so `import_database` mainly adds the guard +
  the readiness flip.
- `services/db_import_export.py` — `_user_to_dict`/`_dict_to_user` (enum via
  `.value`/`UserRole(...)`, datetimes via isoformat helpers, nullable via
  `d.get`) and the `("users", User, ...)` tuple **first** in `TABLE_REGISTRY`.
  Mirror this; the codec **includes** `pwdhash`/`jwt_signing_key` (credentials)
  so restored accounts authenticate.

## Substrate this step binds to (feature-wide — see `context.md`)
- Auth primitives from step 001: `hash_password`, `generate_signing_key`
  (used to build the admin `User`).
- `db.users.create` from step 001 (persistence); the setup service must not touch
  a session directly.
- `is_db_ready()`/`set_db_ready()` from step 002 (the already-configured guard
  and the post-success flip).
- `import_all`/`export_all`/`init_db` from 001 (the import/export mechanism).

## Test isolation note
`test_setup` exercises the process-global readiness flag; rely on the step-002
conftest autouse reset (`set_db_ready(False)` before each test) so
already-configured (`US-001.AC-2`) and unconfigured cases don't cross-contaminate.

## Min length = 8
The `US-001.AC-3` `_TBD` is resolved to **8** (decision 7) — the validation
constant. Recorded in `outcome.md` for back-propagation to `docs/product/`.

See `context.md` for the import/export contract, the readiness flag, and the
feature-wide layering/auth rules.
