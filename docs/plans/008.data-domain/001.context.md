# Step 001 — step-specific context

Field specs and codec exceptions for this step. Shared conventions (snowflake
PK, FK style, codec mechanics, register seam, registry invariant) live in
`context.md`.

## `AssistantMode` (table `assistant_modes`) — `assistant-config.md`

**Realizes:** FEAT-020, UC-095, US-110.

| Field | Type / notes |
|---|---|
| `key` | **PK**, `str`, `Field(primary_key=True)` — natural key, NOT snowflake |
| `system_prompt` | nullable text; null-or-empty is valid |
| `created_at` / `modified_at` | nullable, app-set |

Valid `key` values (the fixed five, enforced by the seed in step 003, not by a
DB constraint here): `edit-character`, `edit-location`, `edit-fact`,
`write-chapter`, `close-chapter`.

**Codec exception:** emit `"key": row.key` verbatim; parse `key=data["key"]`
verbatim. Never route `key` through `int()`.

## `SubAgent` (table `sub_agents`) — `assistant-config.md`

**Realizes:** FEAT-020, UC-096, US-113, US-114.

| Field | Type / notes |
|---|---|
| `id` | snowflake PK (`default_factory=generate_id`) |
| `name` | `str`, `Field(unique=True, index=True)`; non-blank is a service rule (020), not a DB constraint |
| `system_prompt` | text |
| `disabled` | `bool = False` |
| `llm_server_id` | nullable FK → `llm_servers.id` (`int | None`, `Field(default=None, foreign_key="llm_servers.id")`) |
| `model_name` | nullable `str` |
| `created_at` / `modified_at` | nullable, app-set |

`llm_servers` is the existing `LlmServer` table (feature 004,
`models/llm_server.py`) — its tablename is `llm_servers`.

## Registry position

Insert `("assistant_modes", ...)` then `("sub_agents", ...)` immediately after
the existing `llm_servers` tuple. Nothing book-domain exists yet, so they are
positions 3 and 4.
