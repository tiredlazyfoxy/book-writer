# Step 005 — step-specific context

Frozen design in `context.md`: D9 (the full frontend MobX shape, the file list, the
UI flow) and D2 (the `"llama-swap" | "openai"` union mirrored on the TS side). The
Admin SPA substrate (`App.tsx` token-gate + minimal layout, `routes.tsx`,
`api/client.ts` `request<T>`) is 002/004/005's.

Step-local notes:

- **No test files** — there is no frontend test runner (002 decision 5); every DoD
  item is `[manual/live]`, recorded by the verifier as requires-live-run. The only
  automated gate is `npm run build` / `npx tsc --noEmit` (typecheck), which the coder
  runs.
- **`App.tsx` and `routes.tsx` are 005-feature files amended here** — add the nav link
  + route only; **keep** the 004 mount-time token-gate and the minimal local layout.
  This is flagged incremental growth (005 explicitly deferred nav growth to 006), not
  scope drift. Do NOT introduce a shared `AppLayout`/`AppSidebar`/`AppHeader` system.
- **`LlmServersPage.tsx` is created here and amended by step 006** (modal wiring) —
  leave a clean seam for the "New server" / per-row "Select Models" / "Set Embedding"
  actions; step 006 adds the modals and their open/target `useState`.
- **`enabled_models` is `string[]`** on the wire (backend already decodes the JSON) —
  the row badge shows its count.
- **MobX exactness:** `makeAutoObservable` in the constructor; state has no
  `load()`/`delete()` methods; refresh after a mutation = re-call `loadServers`. Modal
  open/target flags are **not** page state (they arrive in step 006 as component-local
  `useState`).
- Reference SPA (adapt, don't copy):
  `D:/GitRoot/_TextGens/LLMRPTextOnlyProject/frontend/src/{types/llmServer.d.ts,api/llmServers.ts,admin/pages/LlmServersPage.tsx,admin/pages/llmServersPageState.ts}`.
  BookWriter uses `has_api_key` presence badges (never a key value) and int ids.
