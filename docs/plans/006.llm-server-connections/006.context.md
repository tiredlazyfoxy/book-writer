# Step 006 — step-specific context

Frozen design in `context.md`: D9 (the three modals, the probe→enable→designate UI
flow, drafts-in-state + `get`-computed validation + separate `serverErrors`, modal
open/target as component-local `useState`) and D2 (the backend-type option list on the
select). The page, page state, api module, and types are step 005's.

Step-local notes:

- **No test files** — all `[manual/live]`; the only automated gate is `npm run build` /
  `npx tsc --noEmit`.
- **`LlmServersPage.tsx` is amended here** (step 005 created it) — wire the modals and
  their open/target `useState`, and reload the list after each mutation. Flagged
  incremental edit, not scope drift.
- **Modal drafts live in component-local state** (per `frontend.md`: modal-dialog
  drafts may live in component-local state), not in `LlmServersPageState`. Each draft
  is a small `<Component>State`-style class with observable fields + pure `get`
  computeds (`isValid`/`canSubmit`) + a separate `serverErrors` merged in the errors
  getter. No Mantine `useForm`.
- **Failed-probe resilience (D9):** `ModelsModal` renders `available ∪ already-enabled`
  so a probe error keeps prior selections visible; `probeModels` runs on open via the
  modal's own async trio and its error is shown in-modal (does not clear the checkbox
  state). `EmbeddingModal` likewise probes on open with its own trio.
- **The `setEmbedding` call replaces the prior designation** (backend enforces
  clear-all-then-set, D5); the UI just calls it and reloads — no client-side clearing
  needed.
- Reference SPA (adapt, don't copy): the reference's LLM-server modal components under
  `D:/GitRoot/_TextGens/LLMRPTextOnlyProject/frontend/src/admin/` — BookWriter uses the
  drafts-in-state + external-effect pattern and never surfaces the raw key.
