# Step 001 — step-specific context

Frozen design and substrate are in `context.md` (D1, D5, D6, the engine
registration hook, the ordered `TABLE_REGISTRY`, the isoformat datetime helper).

Step-local notes:

- **`db/engine.py` is amended by one line only** — the registration hook import
  `import app.models.llm_server  # noqa: F401`. Everything else in `engine.py`
  (`DbConfig`, `init_engine`, `init_db`, `get_standalone_session`) is 001/003's and
  stays untouched. This is a flagged incremental amendment, not scope drift.
- **`db_import_export.py` is amended** to add the codec pair + one registry tuple;
  the existing `users` codec, `BATCH_SIZE`, `export_all`, `import_all`, and the
  isoformat helper are 001/003's — reuse the helper, do not duplicate it.
- **`clear_all_embedding` is the one place raw `sqlalchemy.update()` is allowed
  inside `db/`** (D5) — it is a db-layer function, so it does not breach the
  "no `select()`/`session.exec()`/`session.add()` outside db/" rule (that rule bars
  those primitives *outside* `db/`; `db/` is exactly where sessions and SQLAlchemy
  live).
- **`delete` returning `bool`** is the codebase's first delete; `False` means "no
  row matched that id" (used by step 004's route to distinguish 404).
- `enabled_models` is a **JSON string column**, not a list — the db layer stores and
  returns the raw string; decoding to `list[str]` is the service's job (step 002),
  not this step's.
- Reference (adapt, don't copy): `D:/GitRoot/_TextGens/LLMRPTextOnlyProject/backend`
  — its LLM-server model/db module and its import/export registry. Divergences: int
  id (D1), verbatim api_key export (D6).
